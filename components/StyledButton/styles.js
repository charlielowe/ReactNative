import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        width: '100%',
        padding: '10px',
    
    },

    button: {
       
    },

    text: {
        color: 'white',
    }
});

export default styles;